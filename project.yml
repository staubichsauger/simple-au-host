name: SimpleAUHost
options:
  createIntermediateGroups: true
settings:
  base:
    PRODUCT_BUNDLE_IDENTIFIER: dev.staubichsauger.SimpleAUHost
    MACOSX_DEPLOYMENT_TARGET: 14.0
    SWIFT_VERSION: 6.0
    GENERATE_INFOPLIST_FILE: NO
    INFOPLIST_FILE: SimpleAUHost/Info.plist
    CODE_SIGN_STYLE: Automatic
targets:
  SimpleAUHost:
    type: application
    platform: macOS
    sources:
      - path: SimpleAUHost
      - path: SimpleAUHost/Assets.xcassets
        buildPhase: resources
    settings:
      base:
        PRODUCT_NAME: SimpleAUHost
        SWIFT_OBJC_BRIDGING_HEADER: SimpleAUHost/Support/SimpleAUHost-Bridging-Header.h
        OTHER_CFLAGS: "$(inherited) -std=c11"
    dependencies:
      - sdk: AVFoundation.framework
      - sdk: AudioToolbox.framework
      - sdk: CoreAudio.framework
